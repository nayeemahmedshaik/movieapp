import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.test.movieapp',
  appName: 'movieApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
